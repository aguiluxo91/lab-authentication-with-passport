<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>

<ul>
        {{#if currentUser}}
          <li class="nav-item">
            <form action="/logout" method="POST">
              <span class="mr-3 text-muted">{{currentUser.username}}</span>
              <button type="submit" class="btn btn-danger btn-sm" href="/logout">Logout</button>
            </form>
          </li>
        {{/if}}

</ul>